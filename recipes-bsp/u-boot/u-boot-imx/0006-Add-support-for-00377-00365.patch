From 9d6277218fa1eca0bd1281776397cb803ef1e6f6 Mon Sep 17 00:00:00 2001
From: Tim Carney <tim.carney@cargt.com>
Date: Wed, 3 Dec 2025 14:17:48 -0600
Subject: [PATCH] Add support for 00377-00365.

---
 .../dts/imx8mp-cargt-00377-00365-u-boot.dtsi  |  24 +++
 arch/arm/dts/imx8mp-cargt-00377-00365.dts     | 165 +++++++++++++++++-
 arch/arm/mach-imx/spl.c                       |   7 -
 arch/arm/mach-imx/spl_imx_romapi.c            |  24 +--
 board/cargt/imx8mp_00377/imx8mp_00377.c       |  29 +--
 board/cargt/imx8mp_00377/spl.c                |  18 +-
 common/spl/spl.c                              |   6 +-
 common/spl/spl_bootrom.c                      |   1 -
 common/spl/spl_fit.c                          |  46 +----
 configs/imx8mp-cargt-00377-00365_defconfig    |   2 +-
 include/configs/imx8mp_00377.h                |  14 +-
 11 files changed, 221 insertions(+), 115 deletions(-)

diff --git a/arch/arm/dts/imx8mp-cargt-00377-00365-u-boot.dtsi b/arch/arm/dts/imx8mp-cargt-00377-00365-u-boot.dtsi
index 30f686e0273..a794aeb2f6e 100644
--- a/arch/arm/dts/imx8mp-cargt-00377-00365-u-boot.dtsi
+++ b/arch/arm/dts/imx8mp-cargt-00377-00365-u-boot.dtsi
@@ -178,3 +178,27 @@
 	assigned-clock-parents = <&clk IMX8MP_SYS_PLL1_800M>;
 	assigned-clock-rates = <400000000>;
 };
+
+&ethphy0 {
+	phy-reset-gpios = <&pca9555_20 0 GPIO_ACTIVE_LOW>;
+	reset-assert-us = <15000>;
+	reset-deassert-us = <100000>;
+};
+
+&eqos {
+	reset-gpios = <&pca9555_20 0 GPIO_ACTIVE_LOW>;
+	reset-assert-us = <15000>;
+	reset-deassert-us = <100000>;
+};
+
+&ethphy1 {
+	phy-reset-gpios = <&pca9555_20 1 GPIO_ACTIVE_LOW>;
+	reset-assert-us = <15000>;
+	reset-deassert-us = <100000>;
+};
+
+&fec {
+	phy-reset-gpios = <&pca9555_20 1 GPIO_ACTIVE_LOW>;
+	phy-reset-duration = <15>;
+	phy-reset-post-delay = <100>;
+};
diff --git a/arch/arm/dts/imx8mp-cargt-00377-00365.dts b/arch/arm/dts/imx8mp-cargt-00377-00365.dts
index b2a7f599ed3..8b1387338e0 100644
--- a/arch/arm/dts/imx8mp-cargt-00377-00365.dts
+++ b/arch/arm/dts/imx8mp-cargt-00377-00365.dts
@@ -184,11 +184,126 @@ model = "Cargt i.MX8MP 00377 OSM-L SOM on 00365 carrier board";
 	status = "okay";
 };
 
+&eqos {
+	pinctrl-names = "default";
+	pinctrl-0 = <&pinctrl_eqos>;
+	phy-mode = "rgmii-id";
+	phy-handle = <&ethphy0>;
+	snps,force_thresh_dma_mode;
+	snps,mtl-tx-config = <&mtl_tx_setup>;
+	snps,mtl-rx-config = <&mtl_rx_setup>;
+	status = "okay";
+
+	mdio {
+		compatible = "snps,dwmac-mdio";
+		#address-cells = <1>;
+		#size-cells = <0>;
+
+		ethphy0: ethernet-phy@2 {
+			compatible = "ethernet-phy-ieee802.3-c22";
+			reg = <2>;
+			eee-broken-1000t;
+			reset-gpios = <&pca9555_20 0 GPIO_ACTIVE_LOW>;
+			reset-assert-us = <10000>;
+			reset-deassert-us = <80000>;
+		};
+	};
+
+	mtl_tx_setup: tx-queues-config {
+		snps,tx-queues-to-use = <5>;
+		snps,tx-sched-sp;
+
+		queue0 {
+			snps,dcb-algorithm;
+			snps,priority = <0x1>;
+		};
+
+		queue1 {
+			snps,dcb-algorithm;
+			snps,priority = <0x2>;
+		};
+
+		queue2 {
+			snps,dcb-algorithm;
+			snps,priority = <0x4>;
+		};
+
+		queue3 {
+			snps,dcb-algorithm;
+			snps,priority = <0x8>;
+		};
+
+		queue4 {
+			snps,dcb-algorithm;
+			snps,priority = <0xf0>;
+		};
+	};
+
+	mtl_rx_setup: rx-queues-config {
+		snps,rx-queues-to-use = <5>;
+		snps,rx-sched-sp;
+
+		queue0 {
+			snps,dcb-algorithm;
+			snps,priority = <0x1>;
+			snps,map-to-dma-channel = <0>;
+		};
+
+		queue1 {
+			snps,dcb-algorithm;
+			snps,priority = <0x2>;
+			snps,map-to-dma-channel = <1>;
+		};
+
+		queue2 {
+			snps,dcb-algorithm;
+			snps,priority = <0x4>;
+			snps,map-to-dma-channel = <2>;
+		};
+
+		queue3 {
+			snps,dcb-algorithm;
+			snps,priority = <0x8>;
+			snps,map-to-dma-channel = <3>;
+		};
+
+		queue4 {
+			snps,dcb-algorithm;
+			snps,priority = <0xf0>;
+			snps,map-to-dma-channel = <4>;
+		};
+	};
+};
+
+&fec {
+	pinctrl-names = "default";
+	pinctrl-0 = <&pinctrl_fec>;
+	phy-mode = "rgmii-id";
+	phy-handle = <&ethphy1>;
+	fsl,magic-packet;
+	status = "okay";
+
+	mdio {
+		#address-cells = <1>;
+		#size-cells = <0>;
+
+		ethphy1: ethernet-phy@1 {
+			compatible = "ethernet-phy-ieee802.3-c22";
+			reg = <1>;
+			eee-broken-1000t;
+			reset-gpios = <&pca9555_20 1 GPIO_ACTIVE_LOW>;
+			reset-assert-us = <10000>;
+			reset-deassert-us = <80000>;
+		};
+	};
+};
+
+
 &iomuxc {
 	pinctrl_usdhc2: usdhc2grp {
 		fsl,pins = <
-			MX8MP_IOMUXC_SD2_CLK__USDHC2_CLK	0x190
-			MX8MP_IOMUXC_SD2_CMD__USDHC2_CMD	0x1d0
+			MX8MP_IOMUXC_SD2_CLK__USDHC2_CLK		0x190
+			MX8MP_IOMUXC_SD2_CMD__USDHC2_CMD		0x1d0
 			MX8MP_IOMUXC_SD2_DATA0__USDHC2_DATA0	0x1d0
 			MX8MP_IOMUXC_SD2_DATA1__USDHC2_DATA1	0x1d0
 			MX8MP_IOMUXC_SD2_DATA2__USDHC2_DATA2	0x1d0
@@ -199,8 +314,8 @@ model = "Cargt i.MX8MP 00377 OSM-L SOM on 00365 carrier board";
 
 	pinctrl_usdhc2_100mhz: usdhc2-100mhzgrp {
 		fsl,pins = <
-			MX8MP_IOMUXC_SD2_CLK__USDHC2_CLK	0x194
-			MX8MP_IOMUXC_SD2_CMD__USDHC2_CMD	0x1d4
+			MX8MP_IOMUXC_SD2_CLK__USDHC2_CLK		0x194
+			MX8MP_IOMUXC_SD2_CMD__USDHC2_CMD		0x1d4
 			MX8MP_IOMUXC_SD2_DATA0__USDHC2_DATA0	0x1d4
 			MX8MP_IOMUXC_SD2_DATA1__USDHC2_DATA1	0x1d4
 			MX8MP_IOMUXC_SD2_DATA2__USDHC2_DATA2	0x1d4
@@ -211,8 +326,8 @@ model = "Cargt i.MX8MP 00377 OSM-L SOM on 00365 carrier board";
 
 	pinctrl_usdhc2_200mhz: usdhc2-200mhzgrp {
 		fsl,pins = <
-			MX8MP_IOMUXC_SD2_CLK__USDHC2_CLK	0x196
-			MX8MP_IOMUXC_SD2_CMD__USDHC2_CMD	0x1d6
+			MX8MP_IOMUXC_SD2_CLK__USDHC2_CLK		0x196
+			MX8MP_IOMUXC_SD2_CMD__USDHC2_CMD		0x1d6
 			MX8MP_IOMUXC_SD2_DATA0__USDHC2_DATA0	0x1d6
 			MX8MP_IOMUXC_SD2_DATA1__USDHC2_DATA1	0x1d6
 			MX8MP_IOMUXC_SD2_DATA2__USDHC2_DATA2	0x1d6
@@ -251,5 +366,43 @@ model = "Cargt i.MX8MP 00377 OSM-L SOM on 00365 carrier board";
 			MX8MP_IOMUXC_I2C2_SDA__I2C2_SDA		0x400001c2
 		>;
 	};
+
+	pinctrl_eqos: eqosgrp {
+		fsl,pins = <
+			MX8MP_IOMUXC_ENET_MDC__ENET_QOS_MDC							0x2
+			MX8MP_IOMUXC_ENET_MDIO__ENET_QOS_MDIO						0x2
+			MX8MP_IOMUXC_ENET_RD0__ENET_QOS_RGMII_RD0					0x90
+			MX8MP_IOMUXC_ENET_RD1__ENET_QOS_RGMII_RD1					0x90
+			MX8MP_IOMUXC_ENET_RD2__ENET_QOS_RGMII_RD2					0x90
+			MX8MP_IOMUXC_ENET_RD3__ENET_QOS_RGMII_RD3					0x90
+			MX8MP_IOMUXC_ENET_RXC__CCM_ENET_QOS_CLOCK_GENERATE_RX_CLK	0x90
+			MX8MP_IOMUXC_ENET_RX_CTL__ENET_QOS_RGMII_RX_CTL				0x90
+			MX8MP_IOMUXC_ENET_TD0__ENET_QOS_RGMII_TD0					0x16
+			MX8MP_IOMUXC_ENET_TD1__ENET_QOS_RGMII_TD1					0x16
+			MX8MP_IOMUXC_ENET_TD2__ENET_QOS_RGMII_TD2					0x16
+			MX8MP_IOMUXC_ENET_TD3__ENET_QOS_RGMII_TD3					0x16
+			MX8MP_IOMUXC_ENET_TX_CTL__ENET_QOS_RGMII_TX_CTL				0x16
+			MX8MP_IOMUXC_ENET_TXC__CCM_ENET_QOS_CLOCK_GENERATE_TX_CLK	0x16
+		>;
+	};
+
+	pinctrl_fec: fecgrp {
+		fsl,pins = <
+			MX8MP_IOMUXC_SAI1_RXD2__ENET1_MDC			0x2
+			MX8MP_IOMUXC_SAI1_RXD3__ENET1_MDIO			0x2
+			MX8MP_IOMUXC_SAI1_RXD4__ENET1_RGMII_RD0		0x90
+			MX8MP_IOMUXC_SAI1_RXD5__ENET1_RGMII_RD1		0x90
+			MX8MP_IOMUXC_SAI1_RXD6__ENET1_RGMII_RD2		0x90
+			MX8MP_IOMUXC_SAI1_RXD7__ENET1_RGMII_RD3		0x90
+			MX8MP_IOMUXC_SAI1_TXC__ENET1_RGMII_RXC		0x90
+			MX8MP_IOMUXC_SAI1_TXFS__ENET1_RGMII_RX_CTL	0x90
+			MX8MP_IOMUXC_SAI1_TXD0__ENET1_RGMII_TD0		0x16
+			MX8MP_IOMUXC_SAI1_TXD1__ENET1_RGMII_TD1		0x16
+			MX8MP_IOMUXC_SAI1_TXD2__ENET1_RGMII_TD2		0x16
+			MX8MP_IOMUXC_SAI1_TXD3__ENET1_RGMII_TD3		0x16
+			MX8MP_IOMUXC_SAI1_TXD4__ENET1_RGMII_TX_CTL	0x16
+			MX8MP_IOMUXC_SAI1_TXD5__ENET1_RGMII_TXC		0x16
+		>;
+	};	
  };
   
\ No newline at end of file
diff --git a/arch/arm/mach-imx/spl.c b/arch/arm/mach-imx/spl.c
index fd0ebc4a8ed..68f3e156b32 100644
--- a/arch/arm/mach-imx/spl.c
+++ b/arch/arm/mach-imx/spl.c
@@ -5,7 +5,6 @@
  *
  * Author: Tim Harvey <tharvey@gateworks.com>
  */
-#define DEBUG
 
 #include <common.h>
 #include <hang.h>
@@ -519,8 +518,6 @@ int board_spl_fit_post_load(const void *fit, struct spl_image_info *spl_image)
 	int ret;
 #endif
 
-printf("SPL: FIT image loaded to 0x%p\n", fit);
-
 	if (IS_ENABLED(CONFIG_IMX_HAB)) {
 		u32 offset = ALIGN(fdt_totalsize(fit), 0x1000);
 
@@ -537,14 +534,12 @@ printf("SPL: FIT image loaded to 0x%p\n", fit);
 	}
 #if defined(CONFIG_IMX8MP) || defined(CONFIG_IMX8MN)
 #define MCU_RDC_MAGIC "mcu_rdc"
-printf("Handle MCU RDC config\n");
 	memcpy((void *)CONFIG_IMX8M_MCU_RDC_START_CONFIG_ADDR, MCU_RDC_MAGIC, ALIGN(strlen(MCU_RDC_MAGIC), 4));
 	memcpy((void *)CONFIG_IMX8M_MCU_RDC_STOP_CONFIG_ADDR, MCU_RDC_MAGIC, ALIGN(strlen(MCU_RDC_MAGIC), 4));
 	board_handle_rdc_config(spl_image->fdt_addr, "start-config",
 				(void *)(CONFIG_IMX8M_MCU_RDC_START_CONFIG_ADDR + ALIGN(strlen(MCU_RDC_MAGIC), 4)));
 	board_handle_rdc_config(spl_image->fdt_addr, "stop-config",
 				(void *)(CONFIG_IMX8M_MCU_RDC_STOP_CONFIG_ADDR + ALIGN(strlen(MCU_RDC_MAGIC), 4)));
-printf("MCU RDC config handled\n");
 #endif
 
 #ifdef CONFIG_IMX_TRUSTY_OS
@@ -584,8 +579,6 @@ printf("MCU RDC config handled\n");
         }
 #endif
 
-	printf("board_spl_fit_post_load done\n");
-
 	return 0;
 }
 
diff --git a/arch/arm/mach-imx/spl_imx_romapi.c b/arch/arm/mach-imx/spl_imx_romapi.c
index bfd2118e296..8b8d16d911f 100644
--- a/arch/arm/mach-imx/spl_imx_romapi.c
+++ b/arch/arm/mach-imx/spl_imx_romapi.c
@@ -400,16 +400,12 @@ static int spl_romapi_load_image_stream(struct spl_image_info *spl_image,
 
 		ret = rom_api_download_image(p, 0, imagesize);
 
-		printf("rom_api_download_image returned %d\n", ret);
-
 		p += imagesize;
 
 		if (ret != ROM_API_OKAY) {
 			printf("Failure download %d\n", imagesize);
 			return -1;
 		}
-
-		printf("spl_romapi_load_image_stream continuing...\n");
 	}
 
 	if (IS_ENABLED(CONFIG_SPL_LOAD_FIT)) {
@@ -424,12 +420,9 @@ static int spl_romapi_load_image_stream(struct spl_image_info *spl_image,
 		load.read = spl_romapi_read_stream;
 		load.priv = &ss;
 
-		printf("Calling spl_load_simple_fit (%s)\n", spl_image->name);
 		return spl_load_simple_fit(spl_image, &load, (ulong)phdr, phdr);
 	}
 
-	printf("Image total size calc\n");
-
 	total = img_total_size(phdr);
 	total += 3;
 	total &= ~0x3;
@@ -451,12 +444,8 @@ static int spl_romapi_load_image_stream(struct spl_image_info *spl_image,
 	load.read = spl_ram_load_read;
 
 	if (IS_ENABLED(CONFIG_SPL_LOAD_IMX_CONTAINER))
-	{
-		printf("Calling spl_load_imx_container\n");
 		return spl_load_imx_container(spl_image, &load, (ulong)phdr);
-	}
 
-	printf("Unsupported image type\n");
 	return -1;
 }
 
@@ -465,21 +454,14 @@ int board_return_to_bootrom(struct spl_image_info *spl_image,
 {
 	int ret;
 	u32 boot, bstage;
-	printf("Enter board_return_to_bootrom\n");
 
 	ret = rom_api_query_boot_infor(QUERY_BT_DEV, &boot);
 	if (ret != ROM_API_OKAY)
-	{
-		printf("ROMAPI: failure at query_boot_info\n");
 		goto err;
-	}
 
 	ret = rom_api_query_boot_infor(QUERY_BT_STAGE, &bstage);
 	if (ret != ROM_API_OKAY)
-	{
-		printf("ROMAPI: failure at query_boot_info\n");
-		goto err;	
-	}
+		goto err;
 
 	printf("Boot Stage: ");
 
@@ -501,12 +483,8 @@ int board_return_to_bootrom(struct spl_image_info *spl_image,
 	}
 
 	if (is_boot_from_stream_device(boot))
-	{
-		printf("Boot from stream device\n");
 		return spl_romapi_load_image_stream(spl_image, bootdev);
-	}
 
-	printf("Boot from seekable device\n");
 	return spl_romapi_load_image_seekable(spl_image, bootdev, boot);
 err:
 	puts("ROMAPI: failure at query_boot_info\n");
diff --git a/board/cargt/imx8mp_00377/imx8mp_00377.c b/board/cargt/imx8mp_00377/imx8mp_00377.c
index 58f6d43c82a..b1b1e5f4895 100644
--- a/board/cargt/imx8mp_00377/imx8mp_00377.c
+++ b/board/cargt/imx8mp_00377/imx8mp_00377.c
@@ -79,7 +79,6 @@ int board_early_init_f(void)
 
 	init_uart_clk(3);
 
-	printf("Board early init done.\n");
 	return 0;
 }
 
@@ -129,6 +128,8 @@ int ft_board_setup(void *blob, struct bd_info *bd)
 #endif
 
 #ifdef CONFIG_USB_TCPC
+/* USB_TCPC is not implemented in U-Boot for 00377 yet */
+#else
 struct tcpc_port port1;
 struct tcpc_port port2;
 
@@ -186,8 +187,8 @@ int pd_switch_snk_enable(struct tcpc_port *port)
 
 /* Port2 is the power supply, port 1 does not support power */
 struct tcpc_port_config port1_config = {
-	.i2c_bus = 0, /*i2c1*/
-	.addr = 0x50,
+	.i2c_bus = 1, /*i2c2*/
+	.addr = 0x51,
 	.port_type = TYPEC_PORT_UFP,
 	.max_snk_mv = 20000,
 	.max_snk_ma = 3000,
@@ -198,7 +199,7 @@ struct tcpc_port_config port1_config = {
 };
 
 struct tcpc_port_config port2_config = {
-	.i2c_bus = 0, /*i2c1*/
+	.i2c_bus = 1, /*i2c2*/
 	.addr = 0x52,
 	.port_type = TYPEC_PORT_UFP,
 	.max_snk_mv = 20000,
@@ -370,6 +371,8 @@ int board_usb_init(int index, enum usb_init_type init)
 	if (index == 0 && init == USB_INIT_DEVICE) {
 		imx8m_usb_power(index, true);
 #ifdef CONFIG_USB_TCPC
+printf("USB_TCPC is not implemented in U-Boot for 00377 yet\n");
+#else
 		ret = tcpc_setup_ufp_mode(&port1);
 		if (ret)
 			return ret;
@@ -378,6 +381,8 @@ int board_usb_init(int index, enum usb_init_type init)
 		return dwc3_uboot_init(&dwc3_device_data);
 	} else if (index == 0 && init == USB_INIT_HOST) {
 #ifdef CONFIG_USB_TCPC
+printf("USB_TCPC is not implemented in U-Boot for 00377 yet\n");
+#else
 		ret = tcpc_setup_dfp_mode(&port1);
 #endif
 		return ret;
@@ -395,6 +400,8 @@ int board_usb_cleanup(int index, enum usb_init_type init)
 		imx8m_usb_power(index, false);
 	} else if (index == 0 && init == USB_INIT_HOST) {
 #ifdef CONFIG_USB_TCPC
+printf("USB_TCPC is not implemented in U-Boot for 00377 yet\n");
+#else
 		ret = tcpc_disable_src_vbus(&port1);
 #endif
 	}
@@ -403,6 +410,8 @@ int board_usb_cleanup(int index, enum usb_init_type init)
 }
 
 #ifdef CONFIG_USB_TCPC
+/* USB_TCPC is not implemented in U-Boot for 00377 yet */
+#else
 /* Not used so far */
 int board_typec_get_mode(int index)
 {
@@ -429,34 +438,30 @@ int board_typec_get_mode(int index)
 
 int board_init(void)
 {
-	printf("Board init\n");
 #ifdef CONFIG_DM_REGULATOR
-printf("Regulator init\n");
 	regulators_enable_boot_on(false);
 #endif
 
 #ifdef CONFIG_USB_TCPC
-	printf("Type-C PD init\n");
-	setup_typec();
+printf("***************************************************\n");
+printf("USB_TCPC is not implemented in U-Boot for 00377 yet\n");
+printf("***************************************************\n");
+	// setup_typec();
 #endif
 
 #ifdef CONFIG_NAND_MXS
-printf("NAND init\n");	
 	setup_gpmi_nand();
 #endif
 
 #if defined(CONFIG_USB_DWC3) || defined(CONFIG_USB_XHCI_IMX8M)
-	printf("USB init\n");	
 	init_usb_clk();
 #endif
 
-	printf("Board init done.\n");
 	return 0;
 }
 
 int board_late_init(void)
 {
-	printf("Board late init\n");
 #ifdef CONFIG_ENV_IS_IN_MMC
 	board_late_mmc_env_init();
 #endif
diff --git a/board/cargt/imx8mp_00377/spl.c b/board/cargt/imx8mp_00377/spl.c
index 4dcb80517bf..95015d51643 100644
--- a/board/cargt/imx8mp_00377/spl.c
+++ b/board/cargt/imx8mp_00377/spl.c
@@ -157,19 +157,14 @@ void board_init_f(ulong dummy)
 
 	board_early_init_f();
 
-	printf("Board init f finished\n");
-	printf("Timer init\n");
 	timer_init();
-	printf("Timer initialized\n");
-	printf("SPL early init\n");
+
 	ret = spl_early_init();
 	if (ret) {
-		printf("spl_early_init() failed: %d\n", ret);
+		debug("spl_early_init() failed: %d\n", ret);
 		hang();
 	}
-	printf("SPL early init done\n");
 
-	printf("Get clock device\n");
 	ret = uclass_get_device_by_name(UCLASS_CLK,
 					"clock-controller@30380000",
 					&dev);
@@ -178,23 +173,14 @@ void board_init_f(ulong dummy)
 		hang();
 	}
 
-	printf("Console init\n");
 	preloader_console_init();
-	printf("Console initialized\n");
 
-	printf("Enable TZC380\n");
 	enable_tzc380();
-	printf("TZC380 enabled\n");
 
-	printf("init board\n");
 	power_init_board();
-	printf("init board complete\n");
 
 	/* DDR initialization */
-	printf("init DRAM\n");
 	spl_dram_init();
-	printf("DRAM initialized\n");
 
-	printf("Jump to board_init_r\n");
 	board_init_r(NULL, 0);
 }
diff --git a/common/spl/spl.c b/common/spl/spl.c
index a9b78156602..b65c439e7aa 100644
--- a/common/spl/spl.c
+++ b/common/spl/spl.c
@@ -5,7 +5,6 @@
  *
  * Aneesh V <aneesh@ti.com>
  */
-#define DEBUG
 
 #include <common.h>
 #include <bloblist.h>
@@ -678,7 +677,7 @@ void board_init_r(gd_t *dummy1, ulong dummy2)
 	struct spl_image_info spl_image;
 	int ret, os;
 
-	printf(">>" SPL_TPL_PROMPT "board_init_r()\n");
+	debug(">>" SPL_TPL_PROMPT "board_init_r()\n");
 
 	spl_set_bd();
 
@@ -816,10 +815,7 @@ void board_init_r(gd_t *dummy1, ulong dummy2)
 			       ret);
 	}
 
-	printf("Preparing to boot ...\n");
 	spl_board_prepare_for_boot();
-	printf("Jumping to image at 0x%lx...\n",
-	       spl_image.entry_point);
 	jump_to_image(&spl_image);
 }
 
diff --git a/common/spl/spl_bootrom.c b/common/spl/spl_bootrom.c
index e476cfcea28..0eefd39a519 100644
--- a/common/spl/spl_bootrom.c
+++ b/common/spl/spl_bootrom.c
@@ -9,7 +9,6 @@
 __weak int board_return_to_bootrom(struct spl_image_info *spl_image,
 				   struct spl_boot_device *bootdev)
 {
-	printf("weak implementation of board_return_to_bootrom called\n");
 	return 0;
 }
 
diff --git a/common/spl/spl_fit.c b/common/spl/spl_fit.c
index be2ab712674..5ecf589412e 100644
--- a/common/spl/spl_fit.c
+++ b/common/spl/spl_fit.c
@@ -748,34 +748,22 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 	int index = 0;
 	int firmware_node;
 
-	printf("SPL: Loading from FIT at offset 0x%lx\n", offset);
-
 	ret = spl_simple_fit_read(&ctx, info, offset, fit);
 	if (ret < 0)
-	{
-		printf("%s: FIT read failed: %d\n", __func__, ret);
 		return ret;
-	}
 
 	/* skip further processing if requested to enable load-only use cases */
 	if (spl_load_simple_fit_skip_processing())
-	{
-		printf("SPL: Skipping FIT processing as requested\n");
 		return 0;
-	}
 
 	ctx.fit = spl_load_simple_fit_fix_load(ctx.fit);
 	if (ctx.fit == NULL) {
-		printf("%s: FIT fix load failed\n", __func__);
 		return -1;
 	}
 
 	ret = spl_simple_fit_parse(&ctx);
 	if (ret < 0)
-	{
-		printf("%s: FIT parse failed: %d\n", __func__, ret);
-		return ret;		
-	}
+		return ret;
 
 #if defined(CONFIG_IMX_TRUSTY_OS) && !defined(CONFIG_IMX_MATTER_TRUSTY)
 	int rbindex;
@@ -796,7 +784,6 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 	 *   - fall back 'kernel' (e.g. a Falcon-mode OS boot
 	 *   - fall back to using the first 'loadables' entry
 	 */
-	printf("SPL: Finding U-Boot in FIT\n");
 	if (node < 0)
 		node = spl_fit_get_image_node(&ctx, FIT_FIRMWARE_PROP, 0);
 
@@ -804,7 +791,7 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 		node = spl_fit_get_image_node(&ctx, FIT_KERNEL_PROP, 0);
 
 	if (node < 0) {
-		printf("could not find firmware image, trying loadables...\n");
+		debug("could not find firmware image, trying loadables...\n");
 		node = spl_fit_get_image_node(&ctx, "loadables", 0);
 		/*
 		 * If we pick the U-Boot image from "loadables", start at
@@ -813,34 +800,25 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 		index = 1;
 	}
 	if (node < 0) {
-		printf("%s: Cannot find u-boot image node: %d\n",
+		debug("%s: Cannot find u-boot image node: %d\n",
 		      __func__, node);
 		return -1;
 	}
 
-	printf("SPL: Loading U-Boot from FIT\n");
-
 	/* Load the image and set up the spl_image structure */
 	ret = load_simple_fit(info, offset, &ctx, node, spl_image);
 	if (ret)
-	{
-		printf("%s: can't load u-boot image: %d\n",
-		       __func__, ret);	
 		return ret;
-	}
 
-	printf("SPL: Loaded U-Boot from FIT\n");
 	/*
 	 * For backward compatibility, we treat the first node that is
 	 * as a U-Boot image, if no OS-type has been declared.
 	 */
 	if (!spl_fit_image_get_os(ctx.fit, node, &spl_image->os))
-		printf("Image OS is %s\n", genimg_get_os_name(spl_image->os));
+		debug("Image OS is %s\n", genimg_get_os_name(spl_image->os));
 	else if (!IS_ENABLED(CONFIG_SPL_OS_BOOT))
 		spl_image->os = IH_OS_U_BOOT;
 
-	printf("SPL: U-Boot loaded from FIT at address 0x%lx\n",
-		       spl_image->load_addr);
 	/*
 	 * Booting a next-stage U-Boot may require us to append the FDT.
 	 * We allow this to fail, as the U-Boot image might embed its FDT.
@@ -848,11 +826,7 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 	if (os_takes_devicetree(spl_image->os)) {
 		ret = spl_fit_append_fdt(spl_image, info, offset, &ctx);
 		if (ret < 0 && spl_image->os != IH_OS_U_BOOT)
-		{
-			printf("%s: can't append FDT to U-Boot image: %d\n",
-			       __func__, ret);
 			return ret;
-		}
 	}
 
 	firmware_node = node;
@@ -862,11 +836,8 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 
 		node = spl_fit_get_image_node(&ctx, "loadables", index);
 		if (node < 0)
-		{
-			printf("No more loadables found\n");
-			break;	
-		}
-		printf("SPL: Loading loadable index %d from FIT\n", index);
+			break;
+
 		/*
 		 * if the firmware is also a loadable, skip it because
 		 * it already has been loaded. This is typically the case with
@@ -887,7 +858,7 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 			spl_fit_upload_fpga(&ctx, node, &image_info);
 
 		if (!spl_fit_image_get_os(ctx.fit, node, &os_type))
-			printf("Loadable is %s\n", genimg_get_os_name(os_type));
+			debug("Loadable is %s\n", genimg_get_os_name(os_type));
 
 		if (os_takes_devicetree(os_type)) {
 			spl_fit_append_fdt(&image_info, info, offset, &ctx);
@@ -918,8 +889,7 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 		spl_image->entry_point = spl_image->load_addr;
 
 	spl_image->flags |= SPL_FIT_FOUND;
-	printf("SPL: U-Boot entry point at 0x%lx\n",
-		       spl_image->entry_point);
+
 	return board_spl_fit_post_load(ctx.fit, spl_image);
 }
 
diff --git a/configs/imx8mp-cargt-00377-00365_defconfig b/configs/imx8mp-cargt-00377-00365_defconfig
index 88c528e3d56..5d1d101677a 100644
--- a/configs/imx8mp-cargt-00377-00365_defconfig
+++ b/configs/imx8mp-cargt-00377-00365_defconfig
@@ -34,7 +34,7 @@ CONFIG_OF_BOARD_FIXUP=y
 CONFIG_OF_BOARD_SETUP=y
 CONFIG_OF_SYSTEM_SETUP=y
 CONFIG_BOOTCOMMAND="run distro_bootcmd;run bsp_bootcmd"
-CONFIG_DEFAULT_FDT_FILE="imx8mp-cargt-00377-00365.dtb"
+CONFIG_DEFAULT_FDT_FILE="boot/imx8mp-cargt-00377-00365.dtb"
 CONFIG_ARCH_MISC_INIT=y
 CONFIG_BOARD_EARLY_INIT_F=y
 CONFIG_BOARD_LATE_INIT=y
diff --git a/include/configs/imx8mp_00377.h b/include/configs/imx8mp_00377.h
index da00825398f..7d5109524c3 100644
--- a/include/configs/imx8mp_00377.h
+++ b/include/configs/imx8mp_00377.h
@@ -31,7 +31,7 @@
 #define BOOTENV
 #endif
 
-#define JH_ROOT_DTB    "imx8mp-evk-root.dtb"
+#define JH_ROOT_DTB    "imx8mp-cargt-00377-00365.dtb"
 
 #define JAILHOUSE_ENV \
 	"jh_clk= \0 " \
@@ -91,7 +91,7 @@
 	"scriptaddr=0x43500000\0" \
 	"kernel_addr_r=" __stringify(CONFIG_SYS_LOAD_ADDR) "\0" \
 	"bsp_script=boot.scr\0" \
-	"image=Image\0" \
+	"image=boot/Image\0" \
 	"splashimage=0x50000000\0" \
 	"console=ttymxc3,115200\0" \
 	"fdt_addr_r=0x43000000\0"			\
@@ -105,12 +105,13 @@
 	"mmcpart=1\0" \
 	"mmcroot=/dev/mmcblk1p2 rootwait rw\0" \
 	"mmcautodetect=yes\0" \
-	"mmcargs=setenv bootargs ${jh_clk} ${mcore_clk} console=${console} root=${mmcroot}\0 " \
-	"loadbootscript=fatload mmc ${mmcdev}:${mmcpart} ${loadaddr} ${bsp_script};\0" \
+	"mmcargs=setenv bootargs ${jh_clk} console=${console} " \
+		"root=/dev/mmcblk${mmcdev}p${mmcpart} rootwait rw ${cargt_bootargs}\0" \
+	"loadbootscript=${loadcmd} mmc ${mmcdev}:${mmcpart} ${loadaddr} ${script};\0" \
 	"bootscript=echo Running bootscript from mmc ...; " \
 		"source\0" \
-	"loadimage=fatload mmc ${mmcdev}:${mmcpart} ${loadaddr} ${image}\0" \
-	"loadfdt=fatload mmc ${mmcdev}:${mmcpart} ${fdt_addr_r} ${fdtfile}\0" \
+	"loadimage=${loadcmd} mmc ${mmcdev}:${mmcpart} ${loadaddr} ${image}\0" \
+	"loadfdt=${loadcmd} mmc ${mmcdev}:${mmcpart} ${fdt_addr_r} ${fdtfile}\0" \
 	"mmcboot=echo Booting from mmc ...; " \
 		"run mmcargs; " \
 		"if test ${boot_fit} = yes || test ${boot_fit} = try; then " \
@@ -143,6 +144,7 @@
 			"fi; " \
 		"fi;\0" \
 	"bsp_bootcmd=echo Running BSP bootcmd ...; " \
+		"setenv loadcmd ext4load; " \
 		"mmc dev ${mmcdev}; if mmc rescan; then " \
 		   "if run loadbootscript; then " \
 			   "run bootscript; " \

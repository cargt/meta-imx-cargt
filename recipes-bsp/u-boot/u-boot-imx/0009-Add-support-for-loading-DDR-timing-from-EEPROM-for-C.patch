From 232f742d06d233912f3e3d8e7b7db8e3defa891c Mon Sep 17 00:00:00 2001
From: Tim Carney <tim.carney@cargt.com>
Date: Thu, 11 Dec 2025 11:21:40 -0600
Subject: [PATCH] Add support for loading DDR timing from EEPROM for Cargt
 i.MX93 boards.

---
 arch/arm/mach-imx/imx9/Kconfig            |   4 +-
 board/cargt/common/imx9_eeprom.c          | 153 ++++++++++++++++++++++
 board/cargt/common/imx9_eeprom.h          |  72 ++++++++++
 board/cargt/imx93_00324/Kconfig           |  22 +++-
 board/cargt/imx93_00324/Makefile          |   1 +
 board/cargt/imx93_00324/spl-cargt.c       |  54 +++++++-
 board/cargt/imx93_00363/Kconfig           |  29 ++--
 board/cargt/imx93_00363/Makefile          |   1 +
 board/cargt/imx93_00363/spl-cargt.c       |  54 +++++++-
 configs/imx93-cargt-00324-00326_defconfig |  10 +-
 configs/imx93-cargt-00363-00365_defconfig |  13 +-
 11 files changed, 374 insertions(+), 39 deletions(-)
 create mode 100644 board/cargt/common/imx9_eeprom.c
 create mode 100644 board/cargt/common/imx9_eeprom.h

diff --git a/arch/arm/mach-imx/imx9/Kconfig b/arch/arm/mach-imx/imx9/Kconfig
index 36001e5ec02..ec044f3e074 100644
--- a/arch/arm/mach-imx/imx9/Kconfig
+++ b/arch/arm/mach-imx/imx9/Kconfig
@@ -72,13 +72,11 @@ config TARGET_IMX93_00324
 	bool "imx93_00324"
 	select IMX93
 	select IMX9_LPDDR4X
-	select CARGT_IMX93_LPDDR4X_TIMING_BY_NAME
 	
 config TARGET_IMX93_00363
 	bool "imx93_00363"
 	select IMX93
 	select IMX9_LPDDR4X
-	select CARGT_IMX93_LPDDR4X_TIMING_BY_NAME
 	
 config TARGET_IMX95_19X19_EVK
 	bool "imx95_19x19_evk"
@@ -103,6 +101,8 @@ source "board/freescale/imx93_qsb/Kconfig"
 source "board/freescale/imx95_evk/Kconfig"
 source "board/toradex/verdin-imx95/Kconfig"
 source "board/cargt/imx93_00324/Kconfig"
+source "board/cargt/imx93_00359/Kconfig"
 source "board/cargt/imx93_00363/Kconfig"
+
 endif
 
diff --git a/board/cargt/common/imx9_eeprom.c b/board/cargt/common/imx9_eeprom.c
new file mode 100644
index 00000000000..f26723d4584
--- /dev/null
+++ b/board/cargt/common/imx9_eeprom.c
@@ -0,0 +1,153 @@
+// SPDX-License-Identifier: GPL-2.0+
+/*
+ * Copyright (C) 2025 Cargt Inc.
+ *
+ */
+#include <command.h>
+#include <dm.h>
+#include <i2c.h>
+#include <asm/io.h>
+#include <cpu_func.h>
+#include <u-boot/crc.h>
+#include <asm/arch-imx9/ddr.h>
+
+#include "imx9_eeprom.h"
+
+static int cargt_eeprom_get_dev(struct udevice **devp)
+{
+	int ret;
+	struct udevice *bus;
+
+	ret = uclass_get_device_by_name(UCLASS_I2C, CARGT_EEPROM_I2C_NAME, &bus);
+	if (ret) {
+		printf("%s: No EEPROM I2C bus '%s'\n", __func__,
+		       CARGT_EEPROM_I2C_NAME);
+		return ret;
+	}
+
+	ret = dm_i2c_probe(bus, CARGT_EEPROM_I2C_ADDR, 0, devp);
+	if (ret) {
+		printf("%s: I2C EEPROM probe failed\n", __func__);
+		return ret;
+	}
+
+	i2c_set_chip_offset_len(*devp, 1);
+	i2c_set_chip_addr_offset_mask(*devp, 1);
+
+	return 0;
+}
+
+int cargt_eeprom_read(struct cargt_eeprom *e)
+{
+	int ret;
+	struct udevice *dev;
+
+	ret = cargt_eeprom_get_dev(&dev);
+	if (ret) {
+		printf("%s: Failed to detect I2C EEPROM\n", __func__);
+		return ret;
+	}
+
+	/* Read EEPROM contents to memory */
+	ret = dm_i2c_read(dev, 0, (void *)e, sizeof(*e));
+	if (ret) {
+		printf("%s: EEPROM read failed, ret=%d\n", __func__, ret);
+		return ret;
+	}
+
+	return 0;
+}
+
+int cargt_eeprom_get_serial(struct cargt_eeprom *ep, u8 *serial)
+{
+	flush_dcache_all();
+	if (!cargt_eeprom_is_valid(ep))
+		return -1;
+
+	memcpy(&serial, &ep->serial, sizeof(ep->serial));
+
+	return 0;
+}
+
+uint16_t cargt_eeprom_get_dram_size_mb(struct cargt_eeprom *ep)
+{
+	/* No data in EEPROM - return default DRAM size */
+	if (!cargt_eeprom_is_valid(ep)) {
+		return DEFAULT_SDRAM_SIZE_MB;
+	}
+
+	return be16_to_cpu(ep->dramsize_mbytes);
+}
+
+
+uint16_t cargt_eeprom_get_dram_speed_mts(struct cargt_eeprom *ep)
+{
+	/* No data in EEPROM - return default DRAM size */
+	if (!cargt_eeprom_is_valid(ep)) {
+		return DEFAULT_SDRAM_SPEED_MTS;
+	}
+
+	return __be16_to_cpu(ep->dramspeed_mts);
+}
+
+int cargt_eeprom_get_cpn_string(struct cargt_eeprom *ep, char *cpn_buf, size_t buf_size)
+{
+	flush_dcache_all();
+	if (!cargt_eeprom_is_valid(ep))
+		return -1;
+
+	memset(cpn_buf, 0, buf_size);
+	strncpy(cpn_buf, (const char *)ep->cpn,
+		min(sizeof(ep->cpn), buf_size - 1));
+
+	return 0;
+}
+
+void cargt_eeprom_print_prod_info(struct cargt_eeprom *ep)
+{
+	flush_dcache_all();
+
+	if (!cargt_eeprom_is_valid(ep))
+		return;
+
+	printf("Cargt EEPROM Production Information:\n");
+	printf("\tPart number: %s\n", ep->cpn);
+	printf("\tProduction date: %s\n", ep->date);
+	printf("\tSerial number: 0x%08x\n", be32_to_cpu(ep->serial));
+	printf("\tEEPROM struct version: 0x%x\n", ep->struct_ver);
+	printf("\tDRAM size: %d MB\n", __be16_to_cpu(ep->dramsize_mbytes));
+	printf("\tDRAM speed: %d MT/s\n", __be16_to_cpu(ep->dramspeed_mts));
+}
+
+int cargt_eeprom_is_valid(struct cargt_eeprom *ep)
+{
+	u32 crc, crc_offset = offsetof(struct cargt_eeprom, crc32);
+
+	if (be32_to_cpu(ep->magic) != CARGT_EEPROM_MAGIC) {
+		 printf("Invalid EEPROM magic 0x%08x, expected 0x%08x\n",
+			 be32_to_cpu(ep->magic), CARGT_EEPROM_MAGIC);
+		 return 0;
+	}
+
+	if (ep->struct_ver < 1) {
+		printf("Invalid EEPROM struct version 0x%x\n", ep->struct_ver);
+		return 0;
+	}
+
+	crc = crc32(0, (void *)ep, crc_offset);
+
+	if (crc != be32_to_cpu(ep->crc32)) {
+		printf("EEPROM CRC mismatch (%08x != %08x)\n",
+			   crc, be32_to_cpu(ep->crc32));
+		printf("EEPROM hex dump:\n");
+		for (size_t i = 0; i < sizeof(struct cargt_eeprom); i++) {
+			printf("%02x ", ((u8 *)ep)[i]);
+			if ((i + 1) % 16 == 0)
+				printf("\n");
+		}
+		printf("\n");
+		return 0;
+	}
+
+	return 1;
+}
diff --git a/board/cargt/common/imx9_eeprom.h b/board/cargt/common/imx9_eeprom.h
new file mode 100644
index 00000000000..6d95faf14d9
--- /dev/null
+++ b/board/cargt/common/imx9_eeprom.h
@@ -0,0 +1,72 @@
+/* SPDX-License-Identifier: GPL-2.0+ */
+/*
+ * Copyright (C) 2025 Cargt Inc.
+ *
+ */
+
+#ifndef _MX9_CARGT_EEPROM_H_
+#define _MX9_CARGT_EEPROM_H_
+
+#ifdef CONFIG_ARCH_IMX9
+#include <asm/arch-imx9/ddr.h>
+#endif
+
+#define CARGT_EEPROM_MAGIC	0x43524754 /* == HEX("CRGT") */
+
+#define CARGT_EEPROM_I2C_ADDR	0x50
+
+/*
+ * Example EEPROM data for testing and reference.
+ * This array represents a sample EEPROM structure with:
+ * - Magic number ("CRGT")
+ * - Structure size (48 bytes)
+ * - Structure version (1)
+ * - Part number ("105-00359-02") (null terminated)
+ * - Serial number (0x12345678)
+ * - Build date ("20240610") (null terminated)
+ * - DRAM size (1024 MB)
+ * - DRAM speed (0 MT/s)
+ * - Reserved bytes
+ * - Example CRC32
+ *
+ * Usage: For unit tests or as a template for EEPROM programming.
+ *
+ * static const uint8_t cargt_eeprom_example[48] = {
+ *   0x43, 0x52, 0x47, 0x54,             // magic "CRGT"
+ *   0x30, 0x00,                         // size (48 bytes)
+ *   0x01,                               // struct_ver
+ *   '1','0','5','-','0','0','3','5','9','-','0','2',0x00, // cpn
+ *   0x12, 0x34, 0x56, 0x78,             // serial
+ *   '2','0','2','4','0','6','1','0',0x00, // date
+ *   0x00, 0x04,                         // dramsize_mbytes (1024)
+ *   0x00, 0x00,                         // dramspeed_mts (0)
+ *   0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // reserved
+ *   0xE5, 0xB7, 0xA2, 0xC3              // crc32 (example)
+ * };
+ */
+struct __packed cargt_eeprom
+{
+	u32 magic;				/*  0 - magic number       */
+	u16 size;				/*  4 - size of structure in bytes */
+	u8 struct_ver;      	/*  6 - structure version  */
+	u8 cpn[13];				/*  7 - cargt part number (null terminated) xxx-xxxxx-xx */
+	u32 serial;				/* 20 - cargt serial number */
+	u8 date[9];				/* 24 - build date YYYYMMDD (null terminated) */
+	u16 dramsize_mbytes;	/* 33 - DRAM size in MBytes */
+	u16 dramspeed_mts;		/* 35 - DRAM speed in MT/s  */
+	u8 reserved[7];			/* 37 - reserved */
+	u32 crc32;				/* 44 - CRC32 of this structure */
+};
+
+#define CARGT_EEPROM_DATA ((struct cargt_eeprom *)CARGT_EEPROM_DRAM_START)
+
+int cargt_eeprom_read(struct cargt_eeprom *e);
+int cargt_eeprom_get_serial(struct cargt_eeprom *e, u8 *serial);
+int cargt_eeprom_get_dram_size(struct cargt_eeprom *ep, phys_size_t *size);
+int cargt_eeprom_get_dram_speed(struct cargt_eeprom *ep, phys_size_t *speed);
+void cargt_eeprom_print_prod_info(struct cargt_eeprom *e);
+int cargt_eeprom_is_valid(struct cargt_eeprom *ep);
+int cargt_eeprom_get_cpn_string(struct cargt_eeprom *ep, char *cpn_buf, size_t buf_size);
+
+
+#endif /* _MX9_CARGT_EEPROM_H_ */
diff --git a/board/cargt/imx93_00324/Kconfig b/board/cargt/imx93_00324/Kconfig
index fdc1f71f3f9..7a58dc139f0 100644
--- a/board/cargt/imx93_00324/Kconfig
+++ b/board/cargt/imx93_00324/Kconfig
@@ -10,24 +10,32 @@ config SYS_CONFIG_NAME
 	default "imx93_00324"
 
 choice
-	prompt "Select method to choose LPDDR4x timing"
-	default CARGT_IMX93_LPDDR4X_TIMING_BY_NAME
+	prompt "Select method to choose LPDDR4x timing for TARGET_IMX93_00324"
+	default CARGT_IMX93_00324_LPDDR4X_TIMING_BY_NAME
 
-config CARGT_IMX93_LPDDR4X_TIMING_BY_NAME
-	bool "Using CARGT_IMX93_LPDDR4X_TIMING_FILENAME for LPDDR4x Timing selection"
+config CARGT_IMX93_00324_LPDDR4X_TIMING_BY_NAME
+	bool "Using CARGT_IMX93_00324_LPDDR4X_TIMING_FILENAME for LPDDR4x Timing selection"
 	help
 	  Select the LPDDR4x timing based on specified filename
 
-config CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID
+config CARGT_IMX93_00324_LPDDR4X_TIMING_BY_BOARD_ID
 	bool "Using Cargt HW Board ID for LPDDR4x Timing selection"
 	help
 	  Select the LPDDR4x timing based on Board ID
+
+config CARGT_IMX93_00324_LPDDR4X_TIMING_BY_EEPROM
+	bool "Using Cargt EEPROM contents for LPDDR4x Timing selection"
+	help
+	  Select the LPDDR4x timing based on contents of EEPROM on SOM
+
 endchoice
 
-config CARGT_IMX93_LPDDR4X_TIMING_FILENAME
+config CARGT_IMX93_00324_LPDDR4X_TIMING_FILENAME
 	string "Specify the LPDDR4x Timing Filename"
+    depends on CARGT_IMX93_00324_LPDDR4X_TIMING_BY_NAME
 	default "cargt_lpddr4x_1gbyte_timing"
 	help
-	  Name of the timing file to use when CONFIG_CARGT_LPDDR4X_BY_NAME is used.
+	  Name of the timing file to use when CARGT_IMX93_00324_LPDDR4X_TIMING_BY_NAME is used.
+
 
 endif
diff --git a/board/cargt/imx93_00324/Makefile b/board/cargt/imx93_00324/Makefile
index cbb6ccddbff..aae79ef3108 100644
--- a/board/cargt/imx93_00324/Makefile
+++ b/board/cargt/imx93_00324/Makefile
@@ -5,6 +5,7 @@
 #
 
 obj-y += imx93_00324.o
+obj-$(CONFIG_TARGET_IMX93_00324) += ../common/imx9_eeprom.o
 
 ifdef CONFIG_SPL_BUILD
 obj-y += spl-cargt.o
diff --git a/board/cargt/imx93_00324/spl-cargt.c b/board/cargt/imx93_00324/spl-cargt.c
index 85ea365af37..efc03c1caca 100644
--- a/board/cargt/imx93_00324/spl-cargt.c
+++ b/board/cargt/imx93_00324/spl-cargt.c
@@ -24,6 +24,7 @@
 #include <asm/mach-imx/syscounter.h>
 #include <asm/sections.h>
 #include <dm/uclass.h>
+#include <ctype.h>
 #include <dm/device.h>
 #include <dm/uclass-internal.h>
 #include <dm/device-internal.h>
@@ -35,9 +36,12 @@
 #include <power/pca9450.h>
 #include <power/pf0900.h>
 #include <asm/arch/trdc.h>
+#include "../common/imx9_eeprom.h"
 
 DECLARE_GLOBAL_DATA_PTR;
 
+static struct cargt_eeprom eeprom = {0};
+
 int spl_board_boot_device(enum boot_device boot_dev_spl)
 {
 #ifdef CONFIG_SPL_BOOTROM_SUPPORT
@@ -58,13 +62,15 @@ int spl_board_boot_device(enum boot_device boot_dev_spl)
 
 void spl_board_init(void)
 {
+	struct cargt_eeprom *ep = CARGT_EEPROM_DATA;
 	int ret;
 
 	ret = ele_start_rng();
 	if (ret)
 		printf("Fail to start RNG: %d\n", ret);
 
-	puts("Normal Boot\n");
+	/* Copy EEPROM contents to DRAM */
+	memcpy(ep, &eeprom, sizeof(*ep));
 }
 
 extern struct dram_timing_info dram_timing_1866mts;
@@ -74,9 +80,42 @@ void spl_dram_init(void)
 {
 	struct dram_timing_info *ptiming = &dram_timing;
 	struct gpio_desc desc[4];
+	struct udevice *dev;
 	int ret;
 	uint8_t board_id = 0;
-#if IS_ENABLED (CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID)
+
+	struct udevice *i2c_dev;
+	int addr, res;
+
+#if IS_ENABLED(CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_EEPROM)
+	uint16_t dram_size_mbytes = 0;
+	printf("Reading Cargt EEPROM over I2C...\n");
+	cargt_eeprom_read(&eeprom);
+	printf("Cargt EEPROM read complete.\n");
+	printf("Cargt EEPROM contents at address: 0x%p\n", &eeprom);
+	cargt_eeprom_print_prod_info(&eeprom);	
+
+	/* Determine timing based on size */
+	dram_size_mbytes = cargt_eeprom_get_dram_size_mb(&eeprom);
+	switch(dram_size_mbytes)
+	{
+		case 2048:
+			ptiming = &cargt_lpddr4x_2gbyte_timing;
+			printf("\tLoading timing for Cargt 2Gbyte LPDDR4x\r\n");
+			break;
+		case 1024:
+			ptiming = &cargt_lpddr4x_1gbyte_timing;
+			printf("\tLoading timing for Cargt 1Gbyte LPDDR4x\r\n");
+			break;
+		default:
+			printf("Unsupported DRAM size %d MB in EEPROM, defaulting to 1Gbyte timing\r\n", dram_size_mbytes);
+			ptiming = &cargt_lpddr4x_1gbyte_timing;
+			printf("\tLoading timing for Cargt 1Gbyte LPDDR4x\r\n");
+			break;
+	}
+
+#endif /* CONFIG_CARGT_IMX93_00324_LPDDR4X_TIMING_BY_EEPROM */
+#if IS_ENABLED (CONFIG_CARGT_IMX93_00324_LPDDR4X_TIMING_BY_BOARD_ID)
 /* Check HWID pins to determine which dram timing to load */
 
 /*
@@ -150,9 +189,10 @@ BANKS ARE WRONG IN SPL becuase u-boot doesn't use proper alias!
 		printf("\tLoading timing for Cargt 1Gbyte LPDDR4x\r\n");
 		break;
 	}
-#else /* CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID */
-#if IS_ENABLED(CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_NAME)
-static char *cargt_lpddr4t_timing_string = CONFIG_CARGT_IMX93_LPDDR4X_TIMING_FILENAME;
+#endif /* CONFIG_CARGT_IMX93_00324_LPDDR4X_TIMING_BY_BOARD_ID */
+#if IS_ENABLED(CONFIG_CARGT_IMX93_00324_LPDDR4X_TIMING_BY_NAME)
+#ifdef CONFIG_CARGT_IMX93_00324_LPDDR4X_TIMING_FILENAME
+static char *cargt_lpddr4t_timing_string = CONFIG_CARGT_IMX93_00324_LPDDR4X_TIMING_FILENAME;
 #else
 static char *cargt_lpddr4t_timing_string = "default";
 #endif
@@ -168,12 +208,12 @@ static char *cargt_lpddr4t_timing_string = "default";
 		ptiming = &cargt_lpddr4x_1gbyte_timing;
 		printf("\tLoading timing for Cargt 1Gbyte LPDDR4x\r\n");
 	}
-#endif /* CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID */
+#endif /* CONFIG_CARGT_IMX93_00324_LPDDR4X_TIMING_BY_NAME */
 
 	printf("DDR: %uMTS\n", ptiming->fsp_msg[0].drate);
 	ddr_init(ptiming);
 }
-
+	
 #if CONFIG_IS_ENABLED(DM_PMIC_PF0900)
 int power_init_board(void)
 {
diff --git a/board/cargt/imx93_00363/Kconfig b/board/cargt/imx93_00363/Kconfig
index 9cb9f1467c8..b6d05f670ef 100644
--- a/board/cargt/imx93_00363/Kconfig
+++ b/board/cargt/imx93_00363/Kconfig
@@ -10,25 +10,32 @@ config SYS_CONFIG_NAME
 	default "imx93_00363"
 
 choice
-	prompt "Select method to choose LPDDR4x timing"
-	default CARGT_IMX93_LPDDR4X_TIMING_BY_NAME
+	prompt "Select method to choose LPDDR4x timing for TARGET_IMX93_00363"
+	default CARGT_IMX93_00363_LPDDR4X_TIMING_BY_NAME
 
-config CARGT_IMX93_LPDDR4X_TIMING_BY_NAME
-	bool "Using CARGT_IMX93_LPDDR4X_TIMING_FILENAME for LPDDR4x Timing selection"
+config CARGT_IMX93_00363_LPDDR4X_TIMING_BY_NAME
+	bool "Using CARGT_IMX93_00363_LPDDR4X_TIMING_FILENAME for LPDDR4x Timing selection"
 	help
 	  Select the LPDDR4x timing based on specified filename
 
-config CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID
+config CARGT_IMX93_00363_LPDDR4X_TIMING_BY_BOARD_ID
 	bool "Using Cargt HW Board ID for LPDDR4x Timing selection"
 	help
 	  Select the LPDDR4x timing based on Board ID
+
+config CARGT_IMX93_00363_LPDDR4X_TIMING_BY_EEPROM
+	bool "Using Cargt EEPROM contents for LPDDR4x Timing selection"
+	help
+	  Select the LPDDR4x timing based on contents of EEPROM on SOM
+
 endchoice
 
-config CARGT_IMX93_LPDDR4X_TIMING_FILENAME
-    string "Specify the LPDDR4x Timing Filename"
-    depends on CARGT_IMX93_LPDDR4X_TIMING_BY_NAME
-    default "cargt_lpddr4x_1gbyte_timing"
-    help
-      Name of the timing file to use when CONFIG_CARGT_LPDDR4X_BY_NAME is used.
+config CARGT_IMX93_00363_LPDDR4X_TIMING_FILENAME
+	string "Specify the LPDDR4x Timing Filename"
+    depends on CARGT_IMX93_00363_LPDDR4X_TIMING_BY_NAME
+	default "cargt_lpddr4x_1gbyte_timing"
+	help
+	  Name of the timing file to use when CARGT_IMX93_00363_LPDDR4X_TIMING_BY_NAME is used.
+
 
 endif
diff --git a/board/cargt/imx93_00363/Makefile b/board/cargt/imx93_00363/Makefile
index d5cc8394406..e28f86189d3 100644
--- a/board/cargt/imx93_00363/Makefile
+++ b/board/cargt/imx93_00363/Makefile
@@ -5,6 +5,7 @@
 #
 
 obj-y += imx93_00363.o
+obj-$(CONFIG_TARGET_IMX93_00363) += ../common/imx9_eeprom.o
 
 ifdef CONFIG_SPL_BUILD
 obj-y += spl-cargt.o
diff --git a/board/cargt/imx93_00363/spl-cargt.c b/board/cargt/imx93_00363/spl-cargt.c
index 85ea365af37..1dc174c20d6 100644
--- a/board/cargt/imx93_00363/spl-cargt.c
+++ b/board/cargt/imx93_00363/spl-cargt.c
@@ -24,6 +24,7 @@
 #include <asm/mach-imx/syscounter.h>
 #include <asm/sections.h>
 #include <dm/uclass.h>
+#include <ctype.h>
 #include <dm/device.h>
 #include <dm/uclass-internal.h>
 #include <dm/device-internal.h>
@@ -35,9 +36,12 @@
 #include <power/pca9450.h>
 #include <power/pf0900.h>
 #include <asm/arch/trdc.h>
+#include "../common/imx9_eeprom.h"
 
 DECLARE_GLOBAL_DATA_PTR;
 
+static struct cargt_eeprom eeprom = {0};
+
 int spl_board_boot_device(enum boot_device boot_dev_spl)
 {
 #ifdef CONFIG_SPL_BOOTROM_SUPPORT
@@ -58,13 +62,15 @@ int spl_board_boot_device(enum boot_device boot_dev_spl)
 
 void spl_board_init(void)
 {
+	struct cargt_eeprom *ep = CARGT_EEPROM_DATA;
 	int ret;
 
 	ret = ele_start_rng();
 	if (ret)
 		printf("Fail to start RNG: %d\n", ret);
 
-	puts("Normal Boot\n");
+	/* Copy EEPROM contents to DRAM */
+	memcpy(ep, &eeprom, sizeof(*ep));
 }
 
 extern struct dram_timing_info dram_timing_1866mts;
@@ -74,9 +80,42 @@ void spl_dram_init(void)
 {
 	struct dram_timing_info *ptiming = &dram_timing;
 	struct gpio_desc desc[4];
+	struct udevice *dev;
 	int ret;
 	uint8_t board_id = 0;
-#if IS_ENABLED (CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID)
+
+	struct udevice *i2c_dev;
+	int addr, res;
+
+#if IS_ENABLED(CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_EEPROM)
+	uint16_t dram_size_mbytes = 0;
+	printf("Reading Cargt EEPROM over I2C...\n");
+	cargt_eeprom_read(&eeprom);
+	printf("Cargt EEPROM read complete.\n");
+	printf("Cargt EEPROM contents at address: 0x%p\n", &eeprom);
+	cargt_eeprom_print_prod_info(&eeprom);	
+
+	/* Determine timing based on size */
+	dram_size_mbytes = cargt_eeprom_get_dram_size_mb(&eeprom);
+	switch(dram_size_mbytes)
+	{
+		case 2048:
+			ptiming = &cargt_lpddr4x_2gbyte_timing;
+			printf("\tLoading timing for Cargt 2Gbyte LPDDR4x\r\n");
+			break;
+		case 1024:
+			ptiming = &cargt_lpddr4x_1gbyte_timing;
+			printf("\tLoading timing for Cargt 1Gbyte LPDDR4x\r\n");
+			break;
+		default:
+			printf("Unsupported DRAM size %d MB in EEPROM, defaulting to 1Gbyte timing\r\n", dram_size_mbytes);
+			ptiming = &cargt_lpddr4x_1gbyte_timing;
+			printf("\tLoading timing for Cargt 1Gbyte LPDDR4x\r\n");
+			break;
+	}
+
+#endif /* CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_EEPROM */
+#if IS_ENABLED (CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_BOARD_ID)
 /* Check HWID pins to determine which dram timing to load */
 
 /*
@@ -150,9 +189,10 @@ BANKS ARE WRONG IN SPL becuase u-boot doesn't use proper alias!
 		printf("\tLoading timing for Cargt 1Gbyte LPDDR4x\r\n");
 		break;
 	}
-#else /* CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID */
-#if IS_ENABLED(CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_NAME)
-static char *cargt_lpddr4t_timing_string = CONFIG_CARGT_IMX93_LPDDR4X_TIMING_FILENAME;
+#endif /* CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_BOARD_ID */
+#if IS_ENABLED(CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_NAME)
+#ifdef CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_FILENAME
+static char *cargt_lpddr4t_timing_string = CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_FILENAME;
 #else
 static char *cargt_lpddr4t_timing_string = "default";
 #endif
@@ -168,12 +208,12 @@ static char *cargt_lpddr4t_timing_string = "default";
 		ptiming = &cargt_lpddr4x_1gbyte_timing;
 		printf("\tLoading timing for Cargt 1Gbyte LPDDR4x\r\n");
 	}
-#endif /* CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID */
+#endif /* CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_NAME */
 
 	printf("DDR: %uMTS\n", ptiming->fsp_msg[0].drate);
 	ddr_init(ptiming);
 }
-
+	
 #if CONFIG_IS_ENABLED(DM_PMIC_PF0900)
 int power_init_board(void)
 {
diff --git a/configs/imx93-cargt-00324-00326_defconfig b/configs/imx93-cargt-00324-00326_defconfig
index f9dd7fd3dae..d0eeaeef337 100644
--- a/configs/imx93-cargt-00324-00326_defconfig
+++ b/configs/imx93-cargt-00324-00326_defconfig
@@ -207,6 +207,12 @@ CONFIG_SPL_GPIO_HOG=y
 CONFIG_SPL_DM_GPIO=y
 CONFIG_SPL_DM_GPIO_LOOKUP_LABEL=y
 
+CONFIG_CMD_EEPROM=y
+CONFIG_SYS_I2C_EEPROM_BUS=1
+CONFIG_SYS_EEPROM_SIZE=256
+CONFIG_SYS_EEPROM_PAGE_WRITE_BITS=4
+CONFIG_SYS_EEPROM_PAGE_WRITE_DELAY_MS=100
+
 #CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID is not set
-CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_NAME=y
-CONFIG_CARGT_IMX93_LPDDR4X_TIMING_FILENAME="cargt_lpddr4x_2gbyte_timing"
+#CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_NAME is not set
+CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_EEPROM=y
diff --git a/configs/imx93-cargt-00363-00365_defconfig b/configs/imx93-cargt-00363-00365_defconfig
index af3bf873b3e..e7778f00087 100644
--- a/configs/imx93-cargt-00363-00365_defconfig
+++ b/configs/imx93-cargt-00363-00365_defconfig
@@ -207,6 +207,13 @@ CONFIG_SPL_GPIO_HOG=y
 CONFIG_SPL_DM_GPIO=y
 CONFIG_SPL_DM_GPIO_LOOKUP_LABEL=y
 
-#CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_BOARD_ID is not set
-CONFIG_CARGT_IMX93_LPDDR4X_TIMING_BY_NAME=y
-CONFIG_CARGT_IMX93_LPDDR4X_TIMING_FILENAME="cargt_lpddr4x_1gbyte_timing"
+CONFIG_CMD_EEPROM=y
+CONFIG_SYS_I2C_EEPROM_BUS=0
+CONFIG_SYS_EEPROM_SIZE=256
+CONFIG_SYS_EEPROM_PAGE_WRITE_BITS=4
+CONFIG_SYS_EEPROM_PAGE_WRITE_DELAY_MS=100
+
+# CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_BOARD_ID is not set
+# CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_NAME is not set
+# CONFIG_CARGT_IMX93_LPDDR4X_TIMING_FILENAME="cargt_lpddr4x_1gbyte_timing"
+CONFIG_CARGT_IMX93_00363_LPDDR4X_TIMING_BY_EEPROM=y
